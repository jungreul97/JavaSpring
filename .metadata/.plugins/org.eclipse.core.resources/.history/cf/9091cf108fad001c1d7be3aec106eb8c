package com.ict.controller.di.classfile;

import org.springframework.stereotype.Component;

@Component
public class Broadcast {
	// Stage 에 의존하도록 설정
	private Stage stage;
	
	public Broadcast(Stage stage) {
		this.stage = stage;
	}
	
	public void onAir() {
		System.out.println("방송 송출중인 "+perform()+ "가수가 노래를 합니다.");
	}

}
